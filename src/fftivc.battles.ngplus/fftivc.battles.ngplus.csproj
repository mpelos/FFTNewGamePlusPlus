<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0-windows</TargetFramework>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    <LangVersion>preview</LangVersion>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Local dev: deploy straight into the Reloaded-II Mods folder. On CI (RELOADEDIIMODS unset) fall
         back to the default bin/ so the build/publish doesn't target an empty path. -->
    <OutputPath Condition="'$(RELOADEDIIMODS)' != ''">$(RELOADEDIIMODS)/fftivc.battles.ngplus</OutputPath>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="ModConfig.json">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </Content>
    <EmbeddedResource Include="zlibdict.bin" />
    <EmbeddedResource Include="entd\*.bin" />
    <!-- The modloader reads per-mod table overrides from FFTIVC/tables/<enhanced|classic|combined>/.
         Ship that folder verbatim next to the DLL so JobData.xml (Princess innate abilities) applies. -->
    <Content Include="FFTIVC\**\*">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Reloaded.Mod.Interfaces" Version="2.4.0" ExcludeAssets="runtime" />
    <PackageReference Include="Reloaded.Memory" Version="9.4.3" />
    <PackageReference Include="Reloaded.Memory.SigScan.ReloadedII.Interfaces" Version="1.2.0" />
    <PackageReference Include="Reloaded.SharedLib.Hooks" Version="1.9.0" />
    <PackageReference Include="FF16Tools.Files" Version="1.4.1" />
    <!-- Must match the AssemblyVersion of the INSTALLED modloader (1.6.0.0); the modloader provides
         this assembly at runtime (ExcludeAssets=runtime). Bump only if the installed modloader bumps. -->
    <PackageReference Include="fftivc.utility.modloader.Interfaces" Version="1.6.0" ExcludeAssets="runtime" />
  </ItemGroup>

</Project>
